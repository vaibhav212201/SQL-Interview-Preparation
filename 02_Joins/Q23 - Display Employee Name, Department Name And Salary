## Problem

Retrieve employee name, department name and salary.

## SQL Query

```sql
SELECT e.emp_name,
       d.department_name,
       e.salary
FROM Employees e
INNER JOIN Departments d
ON e.dept_id = d.dept_id;
```

## Explanation

The query joins Employees and Departments tables.

It displays employee name, department name and salary together.

## Expected Output

| emp_name | department_name | salary |
|-----------|----------------|---------|
| Rahul | IT | 50000 |
| Aman | HR | 60000 |

(Output may vary based on dataset)

## Concepts Used

- INNER JOIN
- Multiple Column Selection
- Table Aliases
